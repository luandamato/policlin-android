package br.com.policlinsaude.ui.legacy.mapper

import android.util.Log
import br.com.domain.model.*
import br.com.domain.model.HealthInsurancePhotoList
import br.com.policlinsaude.model.*


object PresentationHealthInsurancePhotoMapper {

/*    fun transform(domain: HealthInsurancePhotoList): PresentationHealthInsurancePhotoList{
        val presentation = PresentationHealthInsurancePhotoList()


        presentation.codAcao    = domain.codAcao
        presentation.imgVerso   = domain.imgVerso
        presentation.msgInterna = domain.msgInterna
        presentation.msgExterna = domain.msgExterna

        presentation.listaimgFrente = transform(domain.listaimgFrente!!.toTypedArray())

        Log.d("Andre","valor de   CARTEIRINHA: " +  presentation.listaimgFrente.toString() );

        return presentation
    }

    fun transform(healthInsurancePhoto: Array<HealthInsurancePhoto>): List<PresentationHealthInsurancePhoto> {
        return healthInsurancePhoto.map { domain ->
            PresentationHealthInsurancePhoto( imgFrente = domain.imgFrente, titular = domain.titular, ordem = domain.ordem)
        }
    }
*/

    //Andre



}